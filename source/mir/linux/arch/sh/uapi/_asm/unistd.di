/++
Auto-generated Linux syscall constants for SH
+/
module mir.linux.arch.sh.uapi._asm.unistd;
version(LDC) pragma(LDC_no_moduleinfo);

static if (size_t.sizeof == 4) enum NR_restart_syscall = 0;
static if (size_t.sizeof == 4) enum NR_exit = 1;
static if (size_t.sizeof == 4) enum NR_fork = 2;
static if (size_t.sizeof == 4) enum NR_read = 3;
static if (size_t.sizeof == 4) enum NR_write = 4;
static if (size_t.sizeof == 4) enum NR_open = 5;
static if (size_t.sizeof == 4) enum NR_close = 6;
static if (size_t.sizeof == 4) enum NR_waitpid = 7;
static if (size_t.sizeof == 4) enum NR_creat = 8;
static if (size_t.sizeof == 4) enum NR_link = 9;
static if (size_t.sizeof == 4) enum NR_unlink = 10;
static if (size_t.sizeof == 4) enum NR_execve = 11;
static if (size_t.sizeof == 4) enum NR_chdir = 12;
static if (size_t.sizeof == 4) enum NR_time = 13;
static if (size_t.sizeof == 4) enum NR_mknod = 14;
static if (size_t.sizeof == 4) enum NR_chmod = 15;
static if (size_t.sizeof == 4) enum NR_lchown = 16;
static if (size_t.sizeof == 4) enum NR_oldstat = 18;
static if (size_t.sizeof == 4) enum NR_lseek = 19;
static if (size_t.sizeof == 4) enum NR_getpid = 20;
static if (size_t.sizeof == 4) enum NR_mount = 21;
static if (size_t.sizeof == 4) enum NR_umount = 22;
static if (size_t.sizeof == 4) enum NR_setuid = 23;
static if (size_t.sizeof == 4) enum NR_getuid = 24;
static if (size_t.sizeof == 4) enum NR_stime = 25;
static if (size_t.sizeof == 4) enum NR_ptrace = 26;
static if (size_t.sizeof == 4) enum NR_alarm = 27;
static if (size_t.sizeof == 4) enum NR_oldfstat = 28;
static if (size_t.sizeof == 4) enum NR_pause = 29;
static if (size_t.sizeof == 4) enum NR_utime = 30;
static if (size_t.sizeof == 4) enum NR_access = 33;
static if (size_t.sizeof == 4) enum NR_nice = 34;
static if (size_t.sizeof == 4) enum NR_sync = 36;
static if (size_t.sizeof == 4) enum NR_kill = 37;
static if (size_t.sizeof == 4) enum NR_rename = 38;
static if (size_t.sizeof == 4) enum NR_mkdir = 39;
static if (size_t.sizeof == 4) enum NR_rmdir = 40;
static if (size_t.sizeof == 4) enum NR_dup = 41;
static if (size_t.sizeof == 4) enum NR_pipe = 42;
static if (size_t.sizeof == 4) enum NR_times = 43;
static if (size_t.sizeof == 4) enum NR_brk = 45;
static if (size_t.sizeof == 4) enum NR_setgid = 46;
static if (size_t.sizeof == 4) enum NR_getgid = 47;
static if (size_t.sizeof == 4) enum NR_signal = 48;
static if (size_t.sizeof == 4) enum NR_geteuid = 49;
static if (size_t.sizeof == 4) enum NR_getegid = 50;
static if (size_t.sizeof == 4) enum NR_acct = 51;
static if (size_t.sizeof == 4) enum NR_umount2 = 52;
static if (size_t.sizeof == 4) enum NR_ioctl = 54;
static if (size_t.sizeof == 4) enum NR_fcntl = 55;
static if (size_t.sizeof == 4) enum NR_setpgid = 57;
static if (size_t.sizeof == 4) enum NR_umask = 60;
static if (size_t.sizeof == 4) enum NR_chroot = 61;
static if (size_t.sizeof == 4) enum NR_ustat = 62;
static if (size_t.sizeof == 4) enum NR_dup2 = 63;
static if (size_t.sizeof == 4) enum NR_getppid = 64;
static if (size_t.sizeof == 4) enum NR_getpgrp = 65;
static if (size_t.sizeof == 4) enum NR_setsid = 66;
static if (size_t.sizeof == 4) enum NR_sigaction = 67;
static if (size_t.sizeof == 4) enum NR_sgetmask = 68;
static if (size_t.sizeof == 4) enum NR_ssetmask = 69;
static if (size_t.sizeof == 4) enum NR_setreuid = 70;
static if (size_t.sizeof == 4) enum NR_setregid = 71;
static if (size_t.sizeof == 4) enum NR_sigsuspend = 72;
static if (size_t.sizeof == 4) enum NR_sigpending = 73;
static if (size_t.sizeof == 4) enum NR_sethostname = 74;
static if (size_t.sizeof == 4) enum NR_setrlimit = 75;
static if (size_t.sizeof == 4) enum NR_getrlimit = 76;
static if (size_t.sizeof == 4) enum NR_getrusage = 77;
static if (size_t.sizeof == 4) enum NR_gettimeofday = 78;
static if (size_t.sizeof == 4) enum NR_settimeofday = 79;
static if (size_t.sizeof == 4) enum NR_getgroups = 80;
static if (size_t.sizeof == 4) enum NR_setgroups = 81;
static if (size_t.sizeof == 4) enum NR_symlink = 83;
static if (size_t.sizeof == 4) enum NR_oldlstat = 84;
static if (size_t.sizeof == 4) enum NR_readlink = 85;
static if (size_t.sizeof == 4) enum NR_uselib = 86;
static if (size_t.sizeof == 4) enum NR_swapon = 87;
static if (size_t.sizeof == 4) enum NR_reboot = 88;
static if (size_t.sizeof == 4) enum NR_readdir = 89;
static if (size_t.sizeof == 4) enum NR_mmap = 90;
static if (size_t.sizeof == 4) enum NR_munmap = 91;
static if (size_t.sizeof == 4) enum NR_truncate = 92;
static if (size_t.sizeof == 4) enum NR_ftruncate = 93;
static if (size_t.sizeof == 4) enum NR_fchmod = 94;
static if (size_t.sizeof == 4) enum NR_fchown = 95;
static if (size_t.sizeof == 4) enum NR_getpriority = 96;
static if (size_t.sizeof == 4) enum NR_setpriority = 97;
static if (size_t.sizeof == 4) enum NR_statfs = 99;
static if (size_t.sizeof == 4) enum NR_fstatfs = 100;
static if (size_t.sizeof == 4) enum NR_socketcall = 102;
static if (size_t.sizeof == 4) enum NR_syslog = 103;
static if (size_t.sizeof == 4) enum NR_setitimer = 104;
static if (size_t.sizeof == 4) enum NR_getitimer = 105;
static if (size_t.sizeof == 4) enum NR_stat = 106;
static if (size_t.sizeof == 4) enum NR_lstat = 107;
static if (size_t.sizeof == 4) enum NR_fstat = 108;
static if (size_t.sizeof == 4) enum NR_olduname = 109;
static if (size_t.sizeof == 4) enum NR_vhangup = 111;
static if (size_t.sizeof == 4) enum NR_wait4 = 114;
static if (size_t.sizeof == 4) enum NR_swapoff = 115;
static if (size_t.sizeof == 4) enum NR_sysinfo = 116;
static if (size_t.sizeof == 4) enum NR_ipc = 117;
static if (size_t.sizeof == 4) enum NR_fsync = 118;
static if (size_t.sizeof == 4) enum NR_sigreturn = 119;
static if (size_t.sizeof == 4) enum NR_clone = 120;
static if (size_t.sizeof == 4) enum NR_setdomainname = 121;
static if (size_t.sizeof == 4) enum NR_uname = 122;
static if (size_t.sizeof == 4) enum NR_cacheflush = 123;
static if (size_t.sizeof == 4) enum NR_adjtimex = 124;
static if (size_t.sizeof == 4) enum NR_mprotect = 125;
static if (size_t.sizeof == 4) enum NR_sigprocmask = 126;
static if (size_t.sizeof == 4) enum NR_init_module = 128;
static if (size_t.sizeof == 4) enum NR_delete_module = 129;
static if (size_t.sizeof == 4) enum NR_quotactl = 131;
static if (size_t.sizeof == 4) enum NR_getpgid = 132;
static if (size_t.sizeof == 4) enum NR_fchdir = 133;
static if (size_t.sizeof == 4) enum NR_bdflush = 134;
static if (size_t.sizeof == 4) enum NR_sysfs = 135;
static if (size_t.sizeof == 4) enum NR_personality = 136;
static if (size_t.sizeof == 4) enum NR_setfsuid = 138;
static if (size_t.sizeof == 4) enum NR_setfsgid = 139;
static if (size_t.sizeof == 4) enum NR__llseek = 140;
static if (size_t.sizeof == 4) enum NR_getdents = 141;
static if (size_t.sizeof == 4) enum NR__newselect = 142;
static if (size_t.sizeof == 4) enum NR_flock = 143;
static if (size_t.sizeof == 4) enum NR_msync = 144;
static if (size_t.sizeof == 4) enum NR_readv = 145;
static if (size_t.sizeof == 4) enum NR_writev = 146;
static if (size_t.sizeof == 4) enum NR_getsid = 147;
static if (size_t.sizeof == 4) enum NR_fdatasync = 148;
static if (size_t.sizeof == 4) enum NR__sysctl = 149;
static if (size_t.sizeof == 4) enum NR_mlock = 150;
static if (size_t.sizeof == 4) enum NR_munlock = 151;
static if (size_t.sizeof == 4) enum NR_mlockall = 152;
static if (size_t.sizeof == 4) enum NR_munlockall = 153;
static if (size_t.sizeof == 4) enum NR_sched_setparam = 154;
static if (size_t.sizeof == 4) enum NR_sched_getparam = 155;
static if (size_t.sizeof == 4) enum NR_sched_setscheduler = 156;
static if (size_t.sizeof == 4) enum NR_sched_getscheduler = 157;
static if (size_t.sizeof == 4) enum NR_sched_yield = 158;
static if (size_t.sizeof == 4) enum NR_sched_get_priority_max = 159;
static if (size_t.sizeof == 4) enum NR_sched_get_priority_min = 160;
static if (size_t.sizeof == 4) enum NR_sched_rr_get_interval = 161;
static if (size_t.sizeof == 4) enum NR_nanosleep = 162;
static if (size_t.sizeof == 4) enum NR_mremap = 163;
static if (size_t.sizeof == 4) enum NR_setresuid = 164;
static if (size_t.sizeof == 4) enum NR_getresuid = 165;
static if (size_t.sizeof == 4) enum NR_poll = 168;
static if (size_t.sizeof == 4) enum NR_nfsservctl = 169;
static if (size_t.sizeof == 4) enum NR_setresgid = 170;
static if (size_t.sizeof == 4) enum NR_getresgid = 171;
static if (size_t.sizeof == 4) enum NR_prctl = 172;
static if (size_t.sizeof == 4) enum NR_rt_sigreturn = 173;
static if (size_t.sizeof == 4) enum NR_rt_sigaction = 174;
static if (size_t.sizeof == 4) enum NR_rt_sigprocmask = 175;
static if (size_t.sizeof == 4) enum NR_rt_sigpending = 176;
static if (size_t.sizeof == 4) enum NR_rt_sigtimedwait = 177;
static if (size_t.sizeof == 4) enum NR_rt_sigqueueinfo = 178;
static if (size_t.sizeof == 4) enum NR_rt_sigsuspend = 179;
static if (size_t.sizeof == 4) enum NR_pread64 = 180;
static if (size_t.sizeof == 4) enum NR_pwrite64 = 181;
static if (size_t.sizeof == 4) enum NR_chown = 182;
static if (size_t.sizeof == 4) enum NR_getcwd = 183;
static if (size_t.sizeof == 4) enum NR_capget = 184;
static if (size_t.sizeof == 4) enum NR_capset = 185;
static if (size_t.sizeof == 4) enum NR_sigaltstack = 186;
static if (size_t.sizeof == 4) enum NR_sendfile = 187;
static if (size_t.sizeof == 4) enum NR_vfork = 190;
static if (size_t.sizeof == 4) enum NR_ugetrlimit = 191	/* SuS compliant getrlimit */;
static if (size_t.sizeof == 4) enum NR_mmap2 = 192;
static if (size_t.sizeof == 4) enum NR_truncate64 = 193;
static if (size_t.sizeof == 4) enum NR_ftruncate64 = 194;
static if (size_t.sizeof == 4) enum NR_stat64 = 195;
static if (size_t.sizeof == 4) enum NR_lstat64 = 196;
static if (size_t.sizeof == 4) enum NR_fstat64 = 197;
static if (size_t.sizeof == 4) enum NR_lchown32 = 198;
static if (size_t.sizeof == 4) enum NR_getuid32 = 199;
static if (size_t.sizeof == 4) enum NR_getgid32 = 200;
static if (size_t.sizeof == 4) enum NR_geteuid32 = 201;
static if (size_t.sizeof == 4) enum NR_getegid32 = 202;
static if (size_t.sizeof == 4) enum NR_setreuid32 = 203;
static if (size_t.sizeof == 4) enum NR_setregid32 = 204;
static if (size_t.sizeof == 4) enum NR_getgroups32 = 205;
static if (size_t.sizeof == 4) enum NR_setgroups32 = 206;
static if (size_t.sizeof == 4) enum NR_fchown32 = 207;
static if (size_t.sizeof == 4) enum NR_setresuid32 = 208;
static if (size_t.sizeof == 4) enum NR_getresuid32 = 209;
static if (size_t.sizeof == 4) enum NR_setresgid32 = 210;
static if (size_t.sizeof == 4) enum NR_getresgid32 = 211;
static if (size_t.sizeof == 4) enum NR_chown32 = 212;
static if (size_t.sizeof == 4) enum NR_setuid32 = 213;
static if (size_t.sizeof == 4) enum NR_setgid32 = 214;
static if (size_t.sizeof == 4) enum NR_setfsuid32 = 215;
static if (size_t.sizeof == 4) enum NR_setfsgid32 = 216;
static if (size_t.sizeof == 4) enum NR_pivot_root = 217;
static if (size_t.sizeof == 4) enum NR_mincore = 218;
static if (size_t.sizeof == 4) enum NR_madvise = 219;
static if (size_t.sizeof == 4) enum NR_getdents64 = 220;
static if (size_t.sizeof == 4) enum NR_fcntl64 = 221;
static if (size_t.sizeof == 4) enum NR_gettid = 224;
static if (size_t.sizeof == 4) enum NR_readahead = 225;
static if (size_t.sizeof == 4) enum NR_setxattr = 226;
static if (size_t.sizeof == 4) enum NR_lsetxattr = 227;
static if (size_t.sizeof == 4) enum NR_fsetxattr = 228;
static if (size_t.sizeof == 4) enum NR_getxattr = 229;
static if (size_t.sizeof == 4) enum NR_lgetxattr = 230;
static if (size_t.sizeof == 4) enum NR_fgetxattr = 231;
static if (size_t.sizeof == 4) enum NR_listxattr = 232;
static if (size_t.sizeof == 4) enum NR_llistxattr = 233;
static if (size_t.sizeof == 4) enum NR_flistxattr = 234;
static if (size_t.sizeof == 4) enum NR_removexattr = 235;
static if (size_t.sizeof == 4) enum NR_lremovexattr = 236;
static if (size_t.sizeof == 4) enum NR_fremovexattr = 237;
static if (size_t.sizeof == 4) enum NR_tkill = 238;
static if (size_t.sizeof == 4) enum NR_sendfile64 = 239;
static if (size_t.sizeof == 4) enum NR_futex = 240;
static if (size_t.sizeof == 4) enum NR_sched_setaffinity = 241;
static if (size_t.sizeof == 4) enum NR_sched_getaffinity = 242;
static if (size_t.sizeof == 4) enum NR_io_setup = 245;
static if (size_t.sizeof == 4) enum NR_io_destroy = 246;
static if (size_t.sizeof == 4) enum NR_io_getevents = 247;
static if (size_t.sizeof == 4) enum NR_io_submit = 248;
static if (size_t.sizeof == 4) enum NR_io_cancel = 249;
static if (size_t.sizeof == 4) enum NR_fadvise64 = 250;
static if (size_t.sizeof == 4) enum NR_exit_group = 252;
static if (size_t.sizeof == 4) enum NR_lookup_dcookie = 253;
static if (size_t.sizeof == 4) enum NR_epoll_create = 254;
static if (size_t.sizeof == 4) enum NR_epoll_ctl = 255;
static if (size_t.sizeof == 4) enum NR_epoll_wait = 256;
static if (size_t.sizeof == 4) enum NR_remap_file_pages = 257;
static if (size_t.sizeof == 4) enum NR_set_tid_address = 258;
static if (size_t.sizeof == 4) enum NR_timer_create = 259;
static if (size_t.sizeof == 4) enum NR_timer_settime = (NR_timer_create+1);
static if (size_t.sizeof == 4) enum NR_timer_gettime = (NR_timer_create+2);
static if (size_t.sizeof == 4) enum NR_timer_getoverrun = (NR_timer_create+3);
static if (size_t.sizeof == 4) enum NR_timer_delete = (NR_timer_create+4);
static if (size_t.sizeof == 4) enum NR_clock_settime = (NR_timer_create+5);
static if (size_t.sizeof == 4) enum NR_clock_gettime = (NR_timer_create+6);
static if (size_t.sizeof == 4) enum NR_clock_getres = (NR_timer_create+7);
static if (size_t.sizeof == 4) enum NR_clock_nanosleep = (NR_timer_create+8);
static if (size_t.sizeof == 4) enum NR_statfs64 = 268;
static if (size_t.sizeof == 4) enum NR_fstatfs64 = 269;
static if (size_t.sizeof == 4) enum NR_tgkill = 270;
static if (size_t.sizeof == 4) enum NR_utimes = 271;
static if (size_t.sizeof == 4) enum NR_fadvise64_64 = 272;
static if (size_t.sizeof == 4) enum NR_mbind = 274;
static if (size_t.sizeof == 4) enum NR_get_mempolicy = 275;
static if (size_t.sizeof == 4) enum NR_set_mempolicy = 276;
static if (size_t.sizeof == 4) enum NR_mq_open = 277;
static if (size_t.sizeof == 4) enum NR_mq_unlink = (NR_mq_open+1);
static if (size_t.sizeof == 4) enum NR_mq_timedsend = (NR_mq_open+2);
static if (size_t.sizeof == 4) enum NR_mq_timedreceive = (NR_mq_open+3);
static if (size_t.sizeof == 4) enum NR_mq_notify = (NR_mq_open+4);
static if (size_t.sizeof == 4) enum NR_mq_getsetattr = (NR_mq_open+5);
static if (size_t.sizeof == 4) enum NR_kexec_load = 283;
static if (size_t.sizeof == 4) enum NR_waitid = 284;
static if (size_t.sizeof == 4) enum NR_add_key = 285;
static if (size_t.sizeof == 4) enum NR_request_key = 286;
static if (size_t.sizeof == 4) enum NR_keyctl = 287;
static if (size_t.sizeof == 4) enum NR_ioprio_set = 288;
static if (size_t.sizeof == 4) enum NR_ioprio_get = 289;
static if (size_t.sizeof == 4) enum NR_inotify_init = 290;
static if (size_t.sizeof == 4) enum NR_inotify_add_watch = 291;
static if (size_t.sizeof == 4) enum NR_inotify_rm_watch = 292;
static if (size_t.sizeof == 4) enum NR_migrate_pages = 294;
static if (size_t.sizeof == 4) enum NR_openat = 295;
static if (size_t.sizeof == 4) enum NR_mkdirat = 296;
static if (size_t.sizeof == 4) enum NR_mknodat = 297;
static if (size_t.sizeof == 4) enum NR_fchownat = 298;
static if (size_t.sizeof == 4) enum NR_futimesat = 299;
static if (size_t.sizeof == 4) enum NR_fstatat64 = 300;
static if (size_t.sizeof == 4) enum NR_unlinkat = 301;
static if (size_t.sizeof == 4) enum NR_renameat = 302;
static if (size_t.sizeof == 4) enum NR_linkat = 303;
static if (size_t.sizeof == 4) enum NR_symlinkat = 304;
static if (size_t.sizeof == 4) enum NR_readlinkat = 305;
static if (size_t.sizeof == 4) enum NR_fchmodat = 306;
static if (size_t.sizeof == 4) enum NR_faccessat = 307;
static if (size_t.sizeof == 4) enum NR_pselect6 = 308;
static if (size_t.sizeof == 4) enum NR_ppoll = 309;
static if (size_t.sizeof == 4) enum NR_unshare = 310;
static if (size_t.sizeof == 4) enum NR_set_robust_list = 311;
static if (size_t.sizeof == 4) enum NR_get_robust_list = 312;
static if (size_t.sizeof == 4) enum NR_splice = 313;
static if (size_t.sizeof == 4) enum NR_sync_file_range = 314;
static if (size_t.sizeof == 4) enum NR_tee = 315;
static if (size_t.sizeof == 4) enum NR_vmsplice = 316;
static if (size_t.sizeof == 4) enum NR_move_pages = 317;
static if (size_t.sizeof == 4) enum NR_getcpu = 318;
static if (size_t.sizeof == 4) enum NR_epoll_pwait = 319;
static if (size_t.sizeof == 4) enum NR_utimensat = 320;
static if (size_t.sizeof == 4) enum NR_signalfd = 321;
static if (size_t.sizeof == 4) enum NR_timerfd_create = 322;
static if (size_t.sizeof == 4) enum NR_eventfd = 323;
static if (size_t.sizeof == 4) enum NR_fallocate = 324;
static if (size_t.sizeof == 4) enum NR_timerfd_settime = 325;
static if (size_t.sizeof == 4) enum NR_timerfd_gettime = 326;
static if (size_t.sizeof == 4) enum NR_signalfd4 = 327;
static if (size_t.sizeof == 4) enum NR_eventfd2 = 328;
static if (size_t.sizeof == 4) enum NR_epoll_create1 = 329;
static if (size_t.sizeof == 4) enum NR_dup3 = 330;
static if (size_t.sizeof == 4) enum NR_pipe2 = 331;
static if (size_t.sizeof == 4) enum NR_inotify_init1 = 332;
static if (size_t.sizeof == 4) enum NR_preadv = 333;
static if (size_t.sizeof == 4) enum NR_pwritev = 334;
static if (size_t.sizeof == 4) enum NR_rt_tgsigqueueinfo = 335;
static if (size_t.sizeof == 4) enum NR_perf_event_open = 336;
static if (size_t.sizeof == 4) enum NR_fanotify_init = 337;
static if (size_t.sizeof == 4) enum NR_fanotify_mark = 338;
static if (size_t.sizeof == 4) enum NR_prlimit64 = 339;
static if (size_t.sizeof == 4) enum NR_socket = 340;
static if (size_t.sizeof == 4) enum NR_bind = 341;
static if (size_t.sizeof == 4) enum NR_connect = 342;
static if (size_t.sizeof == 4) enum NR_listen = 343;
static if (size_t.sizeof == 4) enum NR_accept = 344;
static if (size_t.sizeof == 4) enum NR_getsockname = 345;
static if (size_t.sizeof == 4) enum NR_getpeername = 346;
static if (size_t.sizeof == 4) enum NR_socketpair = 347;
static if (size_t.sizeof == 4) enum NR_send = 348;
static if (size_t.sizeof == 4) enum NR_sendto = 349;
static if (size_t.sizeof == 4) enum NR_recv = 350;
static if (size_t.sizeof == 4) enum NR_recvfrom = 351;
static if (size_t.sizeof == 4) enum NR_shutdown = 352;
static if (size_t.sizeof == 4) enum NR_setsockopt = 353;
static if (size_t.sizeof == 4) enum NR_getsockopt = 354;
static if (size_t.sizeof == 4) enum NR_sendmsg = 355;
static if (size_t.sizeof == 4) enum NR_recvmsg = 356;
static if (size_t.sizeof == 4) enum NR_recvmmsg = 357;
static if (size_t.sizeof == 4) enum NR_accept4 = 358;
static if (size_t.sizeof == 4) enum NR_name_to_handle_at = 359;
static if (size_t.sizeof == 4) enum NR_open_by_handle_at = 360;
static if (size_t.sizeof == 4) enum NR_clock_adjtime = 361;
static if (size_t.sizeof == 4) enum NR_syncfs = 362;
static if (size_t.sizeof == 4) enum NR_sendmmsg = 363;
static if (size_t.sizeof == 4) enum NR_setns = 364;
static if (size_t.sizeof == 4) enum NR_process_vm_readv = 365;
static if (size_t.sizeof == 4) enum NR_process_vm_writev = 366;
static if (size_t.sizeof == 4) enum NR_kcmp = 367;
static if (size_t.sizeof == 4) enum NR_finit_module = 368;
static if (size_t.sizeof == 4) enum NR_sched_getattr = 369;
static if (size_t.sizeof == 4) enum NR_sched_setattr = 370;
static if (size_t.sizeof == 4) enum NR_renameat2 = 371;
static if (size_t.sizeof == 4) enum NR_seccomp = 372;
static if (size_t.sizeof == 4) enum NR_getrandom = 373;
static if (size_t.sizeof == 4) enum NR_memfd_create = 374;
static if (size_t.sizeof == 4) enum NR_bpf = 375;
static if (size_t.sizeof == 4) enum NR_execveat = 376;
static if (size_t.sizeof == 4) enum NR_userfaultfd = 377;
static if (size_t.sizeof == 4) enum NR_membarrier = 378;
static if (size_t.sizeof == 4) enum NR_mlock2 = 379;
static if (size_t.sizeof == 4) enum NR_copy_file_range = 380;
static if (size_t.sizeof == 4) enum NR_preadv2 = 381;
static if (size_t.sizeof == 4) enum NR_pwritev2 = 382;
static if (size_t.sizeof == 8) enum NR_restart_syscall = 0;
static if (size_t.sizeof == 8) enum NR_exit = 1;
static if (size_t.sizeof == 8) enum NR_fork = 2;
static if (size_t.sizeof == 8) enum NR_read = 3;
static if (size_t.sizeof == 8) enum NR_write = 4;
static if (size_t.sizeof == 8) enum NR_open = 5;
static if (size_t.sizeof == 8) enum NR_close = 6;
static if (size_t.sizeof == 8) enum NR_waitpid = 7;
static if (size_t.sizeof == 8) enum NR_creat = 8;
static if (size_t.sizeof == 8) enum NR_link = 9;
static if (size_t.sizeof == 8) enum NR_unlink = 10;
static if (size_t.sizeof == 8) enum NR_execve = 11;
static if (size_t.sizeof == 8) enum NR_chdir = 12;
static if (size_t.sizeof == 8) enum NR_time = 13;
static if (size_t.sizeof == 8) enum NR_mknod = 14;
static if (size_t.sizeof == 8) enum NR_chmod = 15;
static if (size_t.sizeof == 8) enum NR_lchown = 16;
static if (size_t.sizeof == 8) enum NR_oldstat = 18;
static if (size_t.sizeof == 8) enum NR_lseek = 19;
static if (size_t.sizeof == 8) enum NR_getpid = 20;
static if (size_t.sizeof == 8) enum NR_mount = 21;
static if (size_t.sizeof == 8) enum NR_umount = 22;
static if (size_t.sizeof == 8) enum NR_setuid = 23;
static if (size_t.sizeof == 8) enum NR_getuid = 24;
static if (size_t.sizeof == 8) enum NR_stime = 25;
static if (size_t.sizeof == 8) enum NR_ptrace = 26;
static if (size_t.sizeof == 8) enum NR_alarm = 27;
static if (size_t.sizeof == 8) enum NR_oldfstat = 28;
static if (size_t.sizeof == 8) enum NR_pause = 29;
static if (size_t.sizeof == 8) enum NR_utime = 30;
static if (size_t.sizeof == 8) enum NR_access = 33;
static if (size_t.sizeof == 8) enum NR_nice = 34;
static if (size_t.sizeof == 8) enum NR_sync = 36;
static if (size_t.sizeof == 8) enum NR_kill = 37;
static if (size_t.sizeof == 8) enum NR_rename = 38;
static if (size_t.sizeof == 8) enum NR_mkdir = 39;
static if (size_t.sizeof == 8) enum NR_rmdir = 40;
static if (size_t.sizeof == 8) enum NR_dup = 41;
static if (size_t.sizeof == 8) enum NR_pipe = 42;
static if (size_t.sizeof == 8) enum NR_times = 43;
static if (size_t.sizeof == 8) enum NR_brk = 45;
static if (size_t.sizeof == 8) enum NR_setgid = 46;
static if (size_t.sizeof == 8) enum NR_getgid = 47;
static if (size_t.sizeof == 8) enum NR_signal = 48;
static if (size_t.sizeof == 8) enum NR_geteuid = 49;
static if (size_t.sizeof == 8) enum NR_getegid = 50;
static if (size_t.sizeof == 8) enum NR_acct = 51;
static if (size_t.sizeof == 8) enum NR_umount2 = 52;
static if (size_t.sizeof == 8) enum NR_ioctl = 54;
static if (size_t.sizeof == 8) enum NR_fcntl = 55;
static if (size_t.sizeof == 8) enum NR_setpgid = 57;
static if (size_t.sizeof == 8) enum NR_umask = 60;
static if (size_t.sizeof == 8) enum NR_chroot = 61;
static if (size_t.sizeof == 8) enum NR_ustat = 62;
static if (size_t.sizeof == 8) enum NR_dup2 = 63;
static if (size_t.sizeof == 8) enum NR_getppid = 64;
static if (size_t.sizeof == 8) enum NR_getpgrp = 65;
static if (size_t.sizeof == 8) enum NR_setsid = 66;
static if (size_t.sizeof == 8) enum NR_sigaction = 67;
static if (size_t.sizeof == 8) enum NR_sgetmask = 68;
static if (size_t.sizeof == 8) enum NR_ssetmask = 69;
static if (size_t.sizeof == 8) enum NR_setreuid = 70;
static if (size_t.sizeof == 8) enum NR_setregid = 71;
static if (size_t.sizeof == 8) enum NR_sigsuspend = 72;
static if (size_t.sizeof == 8) enum NR_sigpending = 73;
static if (size_t.sizeof == 8) enum NR_sethostname = 74;
static if (size_t.sizeof == 8) enum NR_setrlimit = 75;
static if (size_t.sizeof == 8) enum NR_getrlimit = 76;
static if (size_t.sizeof == 8) enum NR_getrusage = 77;
static if (size_t.sizeof == 8) enum NR_gettimeofday = 78;
static if (size_t.sizeof == 8) enum NR_settimeofday = 79;
static if (size_t.sizeof == 8) enum NR_getgroups = 80;
static if (size_t.sizeof == 8) enum NR_setgroups = 81;
static if (size_t.sizeof == 8) enum NR_symlink = 83;
static if (size_t.sizeof == 8) enum NR_oldlstat = 84;
static if (size_t.sizeof == 8) enum NR_readlink = 85;
static if (size_t.sizeof == 8) enum NR_uselib = 86;
static if (size_t.sizeof == 8) enum NR_swapon = 87;
static if (size_t.sizeof == 8) enum NR_reboot = 88;
static if (size_t.sizeof == 8) enum NR_readdir = 89;
static if (size_t.sizeof == 8) enum NR_mmap = 90;
static if (size_t.sizeof == 8) enum NR_munmap = 91;
static if (size_t.sizeof == 8) enum NR_truncate = 92;
static if (size_t.sizeof == 8) enum NR_ftruncate = 93;
static if (size_t.sizeof == 8) enum NR_fchmod = 94;
static if (size_t.sizeof == 8) enum NR_fchown = 95;
static if (size_t.sizeof == 8) enum NR_getpriority = 96;
static if (size_t.sizeof == 8) enum NR_setpriority = 97;
static if (size_t.sizeof == 8) enum NR_statfs = 99;
static if (size_t.sizeof == 8) enum NR_fstatfs = 100;
static if (size_t.sizeof == 8) enum NR_socketcall = 102;
static if (size_t.sizeof == 8) enum NR_syslog = 103;
static if (size_t.sizeof == 8) enum NR_setitimer = 104;
static if (size_t.sizeof == 8) enum NR_getitimer = 105;
static if (size_t.sizeof == 8) enum NR_stat = 106;
static if (size_t.sizeof == 8) enum NR_lstat = 107;
static if (size_t.sizeof == 8) enum NR_fstat = 108;
static if (size_t.sizeof == 8) enum NR_olduname = 109;
static if (size_t.sizeof == 8) enum NR_vhangup = 111;
static if (size_t.sizeof == 8) enum NR_wait4 = 114;
static if (size_t.sizeof == 8) enum NR_swapoff = 115;
static if (size_t.sizeof == 8) enum NR_sysinfo = 116;
static if (size_t.sizeof == 8) enum NR_ipc = 117;
static if (size_t.sizeof == 8) enum NR_fsync = 118;
static if (size_t.sizeof == 8) enum NR_sigreturn = 119;
static if (size_t.sizeof == 8) enum NR_clone = 120;
static if (size_t.sizeof == 8) enum NR_setdomainname = 121;
static if (size_t.sizeof == 8) enum NR_uname = 122;
static if (size_t.sizeof == 8) enum NR_cacheflush = 123;
static if (size_t.sizeof == 8) enum NR_adjtimex = 124;
static if (size_t.sizeof == 8) enum NR_mprotect = 125;
static if (size_t.sizeof == 8) enum NR_sigprocmask = 126;
static if (size_t.sizeof == 8) enum NR_init_module = 128;
static if (size_t.sizeof == 8) enum NR_delete_module = 129;
static if (size_t.sizeof == 8) enum NR_quotactl = 131;
static if (size_t.sizeof == 8) enum NR_getpgid = 132;
static if (size_t.sizeof == 8) enum NR_fchdir = 133;
static if (size_t.sizeof == 8) enum NR_bdflush = 134;
static if (size_t.sizeof == 8) enum NR_sysfs = 135;
static if (size_t.sizeof == 8) enum NR_personality = 136;
static if (size_t.sizeof == 8) enum NR_setfsuid = 138;
static if (size_t.sizeof == 8) enum NR_setfsgid = 139;
static if (size_t.sizeof == 8) enum NR__llseek = 140;
static if (size_t.sizeof == 8) enum NR_getdents = 141;
static if (size_t.sizeof == 8) enum NR__newselect = 142;
static if (size_t.sizeof == 8) enum NR_flock = 143;
static if (size_t.sizeof == 8) enum NR_msync = 144;
static if (size_t.sizeof == 8) enum NR_readv = 145;
static if (size_t.sizeof == 8) enum NR_writev = 146;
static if (size_t.sizeof == 8) enum NR_getsid = 147;
static if (size_t.sizeof == 8) enum NR_fdatasync = 148;
static if (size_t.sizeof == 8) enum NR__sysctl = 149;
static if (size_t.sizeof == 8) enum NR_mlock = 150;
static if (size_t.sizeof == 8) enum NR_munlock = 151;
static if (size_t.sizeof == 8) enum NR_mlockall = 152;
static if (size_t.sizeof == 8) enum NR_munlockall = 153;
static if (size_t.sizeof == 8) enum NR_sched_setparam = 154;
static if (size_t.sizeof == 8) enum NR_sched_getparam = 155;
static if (size_t.sizeof == 8) enum NR_sched_setscheduler = 156;
static if (size_t.sizeof == 8) enum NR_sched_getscheduler = 157;
static if (size_t.sizeof == 8) enum NR_sched_yield = 158;
static if (size_t.sizeof == 8) enum NR_sched_get_priority_max = 159;
static if (size_t.sizeof == 8) enum NR_sched_get_priority_min = 160;
static if (size_t.sizeof == 8) enum NR_sched_rr_get_interval = 161;
static if (size_t.sizeof == 8) enum NR_nanosleep = 162;
static if (size_t.sizeof == 8) enum NR_mremap = 163;
static if (size_t.sizeof == 8) enum NR_setresuid = 164;
static if (size_t.sizeof == 8) enum NR_getresuid = 165;
static if (size_t.sizeof == 8) enum NR_poll = 168;
static if (size_t.sizeof == 8) enum NR_nfsservctl = 169;
static if (size_t.sizeof == 8) enum NR_setresgid = 170;
static if (size_t.sizeof == 8) enum NR_getresgid = 171;
static if (size_t.sizeof == 8) enum NR_prctl = 172;
static if (size_t.sizeof == 8) enum NR_rt_sigreturn = 173;
static if (size_t.sizeof == 8) enum NR_rt_sigaction = 174;
static if (size_t.sizeof == 8) enum NR_rt_sigprocmask = 175;
static if (size_t.sizeof == 8) enum NR_rt_sigpending = 176;
static if (size_t.sizeof == 8) enum NR_rt_sigtimedwait = 177;
static if (size_t.sizeof == 8) enum NR_rt_sigqueueinfo = 178;
static if (size_t.sizeof == 8) enum NR_rt_sigsuspend = 179;
static if (size_t.sizeof == 8) enum NR_pread64 = 180;
static if (size_t.sizeof == 8) enum NR_pwrite64 = 181;
static if (size_t.sizeof == 8) enum NR_chown = 182;
static if (size_t.sizeof == 8) enum NR_getcwd = 183;
static if (size_t.sizeof == 8) enum NR_capget = 184;
static if (size_t.sizeof == 8) enum NR_capset = 185;
static if (size_t.sizeof == 8) enum NR_sigaltstack = 186;
static if (size_t.sizeof == 8) enum NR_sendfile = 187;
static if (size_t.sizeof == 8) enum NR_vfork = 190;
static if (size_t.sizeof == 8) enum NR_ugetrlimit = 191	/* SuS compliant getrlimit */;
static if (size_t.sizeof == 8) enum NR_mmap2 = 192;
static if (size_t.sizeof == 8) enum NR_truncate64 = 193;
static if (size_t.sizeof == 8) enum NR_ftruncate64 = 194;
static if (size_t.sizeof == 8) enum NR_stat64 = 195;
static if (size_t.sizeof == 8) enum NR_lstat64 = 196;
static if (size_t.sizeof == 8) enum NR_fstat64 = 197;
static if (size_t.sizeof == 8) enum NR_lchown32 = 198;
static if (size_t.sizeof == 8) enum NR_getuid32 = 199;
static if (size_t.sizeof == 8) enum NR_getgid32 = 200;
static if (size_t.sizeof == 8) enum NR_geteuid32 = 201;
static if (size_t.sizeof == 8) enum NR_getegid32 = 202;
static if (size_t.sizeof == 8) enum NR_setreuid32 = 203;
static if (size_t.sizeof == 8) enum NR_setregid32 = 204;
static if (size_t.sizeof == 8) enum NR_getgroups32 = 205;
static if (size_t.sizeof == 8) enum NR_setgroups32 = 206;
static if (size_t.sizeof == 8) enum NR_fchown32 = 207;
static if (size_t.sizeof == 8) enum NR_setresuid32 = 208;
static if (size_t.sizeof == 8) enum NR_getresuid32 = 209;
static if (size_t.sizeof == 8) enum NR_setresgid32 = 210;
static if (size_t.sizeof == 8) enum NR_getresgid32 = 211;
static if (size_t.sizeof == 8) enum NR_chown32 = 212;
static if (size_t.sizeof == 8) enum NR_setuid32 = 213;
static if (size_t.sizeof == 8) enum NR_setgid32 = 214;
static if (size_t.sizeof == 8) enum NR_setfsuid32 = 215;
static if (size_t.sizeof == 8) enum NR_setfsgid32 = 216;
static if (size_t.sizeof == 8) enum NR_pivot_root = 217;
static if (size_t.sizeof == 8) enum NR_mincore = 218;
static if (size_t.sizeof == 8) enum NR_madvise = 219;
static if (size_t.sizeof == 8) enum NR_socket = 220;
static if (size_t.sizeof == 8) enum NR_bind = 221;
static if (size_t.sizeof == 8) enum NR_connect = 222;
static if (size_t.sizeof == 8) enum NR_listen = 223;
static if (size_t.sizeof == 8) enum NR_accept = 224;
static if (size_t.sizeof == 8) enum NR_getsockname = 225;
static if (size_t.sizeof == 8) enum NR_getpeername = 226;
static if (size_t.sizeof == 8) enum NR_socketpair = 227;
static if (size_t.sizeof == 8) enum NR_send = 228;
static if (size_t.sizeof == 8) enum NR_sendto = 229;
static if (size_t.sizeof == 8) enum NR_recv = 230;
static if (size_t.sizeof == 8) enum NR_recvfrom = 231;
static if (size_t.sizeof == 8) enum NR_shutdown = 232;
static if (size_t.sizeof == 8) enum NR_setsockopt = 233;
static if (size_t.sizeof == 8) enum NR_getsockopt = 234;
static if (size_t.sizeof == 8) enum NR_sendmsg = 235;
static if (size_t.sizeof == 8) enum NR_recvmsg = 236;
static if (size_t.sizeof == 8) enum NR_semop = 237;
static if (size_t.sizeof == 8) enum NR_semget = 238;
static if (size_t.sizeof == 8) enum NR_semctl = 239;
static if (size_t.sizeof == 8) enum NR_msgsnd = 240;
static if (size_t.sizeof == 8) enum NR_msgrcv = 241;
static if (size_t.sizeof == 8) enum NR_msgget = 242;
static if (size_t.sizeof == 8) enum NR_msgctl = 243;
static if (size_t.sizeof == 8) enum NR_shmat = 244;
static if (size_t.sizeof == 8) enum NR_shmdt = 245;
static if (size_t.sizeof == 8) enum NR_shmget = 246;
static if (size_t.sizeof == 8) enum NR_shmctl = 247;
static if (size_t.sizeof == 8) enum NR_getdents64 = 248;
static if (size_t.sizeof == 8) enum NR_fcntl64 = 249;
static if (size_t.sizeof == 8) enum NR_gettid = 252;
static if (size_t.sizeof == 8) enum NR_readahead = 253;
static if (size_t.sizeof == 8) enum NR_setxattr = 254;
static if (size_t.sizeof == 8) enum NR_lsetxattr = 255;
static if (size_t.sizeof == 8) enum NR_fsetxattr = 256;
static if (size_t.sizeof == 8) enum NR_getxattr = 257;
static if (size_t.sizeof == 8) enum NR_lgetxattr = 258;
static if (size_t.sizeof == 8) enum NR_fgetxattr = 259;
static if (size_t.sizeof == 8) enum NR_listxattr = 260;
static if (size_t.sizeof == 8) enum NR_llistxattr = 261;
static if (size_t.sizeof == 8) enum NR_flistxattr = 262;
static if (size_t.sizeof == 8) enum NR_removexattr = 263;
static if (size_t.sizeof == 8) enum NR_lremovexattr = 264;
static if (size_t.sizeof == 8) enum NR_fremovexattr = 265;
static if (size_t.sizeof == 8) enum NR_tkill = 266;
static if (size_t.sizeof == 8) enum NR_sendfile64 = 267;
static if (size_t.sizeof == 8) enum NR_futex = 268;
static if (size_t.sizeof == 8) enum NR_sched_setaffinity = 269;
static if (size_t.sizeof == 8) enum NR_sched_getaffinity = 270;
static if (size_t.sizeof == 8) enum NR_io_setup = 273;
static if (size_t.sizeof == 8) enum NR_io_destroy = 274;
static if (size_t.sizeof == 8) enum NR_io_getevents = 275;
static if (size_t.sizeof == 8) enum NR_io_submit = 276;
static if (size_t.sizeof == 8) enum NR_io_cancel = 277;
static if (size_t.sizeof == 8) enum NR_fadvise64 = 278;
static if (size_t.sizeof == 8) enum NR_exit_group = 280;
static if (size_t.sizeof == 8) enum NR_lookup_dcookie = 281;
static if (size_t.sizeof == 8) enum NR_epoll_create = 282;
static if (size_t.sizeof == 8) enum NR_epoll_ctl = 283;
static if (size_t.sizeof == 8) enum NR_epoll_wait = 284;
static if (size_t.sizeof == 8) enum NR_remap_file_pages = 285;
static if (size_t.sizeof == 8) enum NR_set_tid_address = 286;
static if (size_t.sizeof == 8) enum NR_timer_create = 287;
static if (size_t.sizeof == 8) enum NR_timer_settime = (NR_timer_create+1);
static if (size_t.sizeof == 8) enum NR_timer_gettime = (NR_timer_create+2);
static if (size_t.sizeof == 8) enum NR_timer_getoverrun = (NR_timer_create+3);
static if (size_t.sizeof == 8) enum NR_timer_delete = (NR_timer_create+4);
static if (size_t.sizeof == 8) enum NR_clock_settime = (NR_timer_create+5);
static if (size_t.sizeof == 8) enum NR_clock_gettime = (NR_timer_create+6);
static if (size_t.sizeof == 8) enum NR_clock_getres = (NR_timer_create+7);
static if (size_t.sizeof == 8) enum NR_clock_nanosleep = (NR_timer_create+8);
static if (size_t.sizeof == 8) enum NR_statfs64 = 296;
static if (size_t.sizeof == 8) enum NR_fstatfs64 = 297;
static if (size_t.sizeof == 8) enum NR_tgkill = 298;
static if (size_t.sizeof == 8) enum NR_utimes = 299;
static if (size_t.sizeof == 8) enum NR_fadvise64_64 = 300;
static if (size_t.sizeof == 8) enum NR_mq_open = 305;
static if (size_t.sizeof == 8) enum NR_mq_unlink = (NR_mq_open+1);
static if (size_t.sizeof == 8) enum NR_mq_timedsend = (NR_mq_open+2);
static if (size_t.sizeof == 8) enum NR_mq_timedreceive = (NR_mq_open+3);
static if (size_t.sizeof == 8) enum NR_mq_notify = (NR_mq_open+4);
static if (size_t.sizeof == 8) enum NR_mq_getsetattr = (NR_mq_open+5);
static if (size_t.sizeof == 8) enum NR_waitid = 312;
static if (size_t.sizeof == 8) enum NR_add_key = 313;
static if (size_t.sizeof == 8) enum NR_request_key = 314;
static if (size_t.sizeof == 8) enum NR_keyctl = 315;
static if (size_t.sizeof == 8) enum NR_ioprio_set = 316;
static if (size_t.sizeof == 8) enum NR_ioprio_get = 317;
static if (size_t.sizeof == 8) enum NR_inotify_init = 318;
static if (size_t.sizeof == 8) enum NR_inotify_add_watch = 319;
static if (size_t.sizeof == 8) enum NR_inotify_rm_watch = 320;
static if (size_t.sizeof == 8) enum NR_migrate_pages = 322;
static if (size_t.sizeof == 8) enum NR_openat = 323;
static if (size_t.sizeof == 8) enum NR_mkdirat = 324;
static if (size_t.sizeof == 8) enum NR_mknodat = 325;
static if (size_t.sizeof == 8) enum NR_fchownat = 326;
static if (size_t.sizeof == 8) enum NR_futimesat = 327;
static if (size_t.sizeof == 8) enum NR_fstatat64 = 328;
static if (size_t.sizeof == 8) enum NR_unlinkat = 329;
static if (size_t.sizeof == 8) enum NR_renameat = 330;
static if (size_t.sizeof == 8) enum NR_linkat = 331;
static if (size_t.sizeof == 8) enum NR_symlinkat = 332;
static if (size_t.sizeof == 8) enum NR_readlinkat = 333;
static if (size_t.sizeof == 8) enum NR_fchmodat = 334;
static if (size_t.sizeof == 8) enum NR_faccessat = 335;
static if (size_t.sizeof == 8) enum NR_pselect6 = 336;
static if (size_t.sizeof == 8) enum NR_ppoll = 337;
static if (size_t.sizeof == 8) enum NR_unshare = 338;
static if (size_t.sizeof == 8) enum NR_set_robust_list = 339;
static if (size_t.sizeof == 8) enum NR_get_robust_list = 340;
static if (size_t.sizeof == 8) enum NR_splice = 341;
static if (size_t.sizeof == 8) enum NR_sync_file_range = 342;
static if (size_t.sizeof == 8) enum NR_tee = 343;
static if (size_t.sizeof == 8) enum NR_vmsplice = 344;
static if (size_t.sizeof == 8) enum NR_move_pages = 345;
static if (size_t.sizeof == 8) enum NR_getcpu = 346;
static if (size_t.sizeof == 8) enum NR_epoll_pwait = 347;
static if (size_t.sizeof == 8) enum NR_utimensat = 348;
static if (size_t.sizeof == 8) enum NR_signalfd = 349;
static if (size_t.sizeof == 8) enum NR_timerfd_create = 350;
static if (size_t.sizeof == 8) enum NR_eventfd = 351;
static if (size_t.sizeof == 8) enum NR_fallocate = 352;
static if (size_t.sizeof == 8) enum NR_timerfd_settime = 353;
static if (size_t.sizeof == 8) enum NR_timerfd_gettime = 354;
static if (size_t.sizeof == 8) enum NR_signalfd4 = 355;
static if (size_t.sizeof == 8) enum NR_eventfd2 = 356;
static if (size_t.sizeof == 8) enum NR_epoll_create1 = 357;
static if (size_t.sizeof == 8) enum NR_dup3 = 358;
static if (size_t.sizeof == 8) enum NR_pipe2 = 359;
static if (size_t.sizeof == 8) enum NR_inotify_init1 = 360;
static if (size_t.sizeof == 8) enum NR_preadv = 361;
static if (size_t.sizeof == 8) enum NR_pwritev = 362;
static if (size_t.sizeof == 8) enum NR_rt_tgsigqueueinfo = 363;
static if (size_t.sizeof == 8) enum NR_perf_event_open = 364;
static if (size_t.sizeof == 8) enum NR_recvmmsg = 365;
static if (size_t.sizeof == 8) enum NR_accept4 = 366;
static if (size_t.sizeof == 8) enum NR_fanotify_init = 367;
static if (size_t.sizeof == 8) enum NR_fanotify_mark = 368;
static if (size_t.sizeof == 8) enum NR_prlimit64 = 369;
static if (size_t.sizeof == 8) enum NR_name_to_handle_at = 370;
static if (size_t.sizeof == 8) enum NR_open_by_handle_at = 371;
static if (size_t.sizeof == 8) enum NR_clock_adjtime = 372;
static if (size_t.sizeof == 8) enum NR_syncfs = 373;
static if (size_t.sizeof == 8) enum NR_sendmmsg = 374;
static if (size_t.sizeof == 8) enum NR_setns = 375;
static if (size_t.sizeof == 8) enum NR_process_vm_readv = 376;
static if (size_t.sizeof == 8) enum NR_process_vm_writev = 377;
static if (size_t.sizeof == 8) enum NR_kcmp = 378;
static if (size_t.sizeof == 8) enum NR_finit_module = 379;
static if (size_t.sizeof == 8) enum NR_sched_getattr = 380;
static if (size_t.sizeof == 8) enum NR_sched_setattr = 381;
static if (size_t.sizeof == 8) enum NR_renameat2 = 382;
static if (size_t.sizeof == 8) enum NR_seccomp = 383;
static if (size_t.sizeof == 8) enum NR_getrandom = 384;
static if (size_t.sizeof == 8) enum NR_memfd_create = 385;
static if (size_t.sizeof == 8) enum NR_bpf = 386;
static if (size_t.sizeof == 8) enum NR_execveat = 387;
static if (size_t.sizeof == 8) enum NR_userfaultfd = 388;
static if (size_t.sizeof == 8) enum NR_membarrier = 389;
static if (size_t.sizeof == 8) enum NR_mlock2 = 390;
static if (size_t.sizeof == 8) enum NR_copy_file_range = 391;
static if (size_t.sizeof == 8) enum NR_preadv2 = 392;
static if (size_t.sizeof == 8) enum NR_pwritev2 = 393;
